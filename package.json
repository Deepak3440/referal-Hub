{
  "name": "referaa",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:backend": "pnpm --filter @referaa/backend run dev",
    "dev:frontend": "pnpm --filter @referaa/frontend run dev",
    "seed:demo": "pnpm --filter @referaa/backend run seed:demo",
    "seed:demo-referrals": "pnpm --filter @referaa/backend run seed:demo-referrals"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
